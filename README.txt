美食雷達 App（前端模擬分檔版）
- 直接打開 index.html 即可在瀏覽器測試。
- 主要檔案：
  - index.html
  - styles.css
  - app.js
- 這是單頁應用（SPA）模擬，所有頁面透過抽屜（sheet）與覆蓋層（overlay）呈現。
- 之後若要接 API，請在 app.js 的 confirmNav 或 startScan 位置串接實際資料。
